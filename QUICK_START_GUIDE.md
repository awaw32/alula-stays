# 🚀 دليل تشغيل الموقع محلياً (Local Development)

## المتطلبات الأساسية

```bash
# تحقق من وجود Node.js
node --version  # يجب أن يكون 18+

# تحقق من وجود npm أو yarn
npm --version
# أو بدلاً منه
bun --version
```

---

## 🎯 خطوات التشغيل السريع

### 1️⃣ **الدخول للمجلد**
```bash
cd "C:\Users\koko4\OneDrive\المستندات\شقق العلا\alula-stays"
```

### 2️⃣ **تثبيت الحزم (إذا لم تُثبت)**
```bash
# باستخدام npm
npm install

# أو باستخدام bun
bun install
```

### 3️⃣ **إعداد متغيرات البيئة**

أنشئ ملف `.env.local` في المجلد الرئيسي:

```env
# Convex
CONVEX_DEPLOYMENT=your_deployment_id
VITE_CONVEX_URL=your_convex_url

# Stripe
STRIPE_SECRET_KEY=sk_test_your_key_here
STRIPE_PUBLISHABLE_KEY=pk_test_your_key_here

# البريد الإلكتروني (SendGrid)
SENDGRID_API_KEY=your_sendgrid_key_here

# التطبيق
SITE_URL=http://localhost:5173
PUBLIC_APP_URL=http://localhost:5173
```

### 4️⃣ **تشغيل الخادم المحلي**

```bash
# تطوير (مع hot reload)
npm run dev
# أو
bun run dev
```

**سيظهر:**
```
Local:   http://localhost:5173
Network: http://192.168.x.x:5173
```

---

## 🌐 فتح الموقع في المتصفح

بعد تشغيل الأمر أعلاه، افتح المتصفح واذهب إلى:

```
http://localhost:5173
```

---

## 📱 اختبر كمستخدم عادي

### 1. **التسجيل الأول**
- اضغط على "إنشاء حساب"
- أدخل بريدك الإلكتروني
- انتظر البريد التأكيدي (قد يكون في Spam)

### 2. **البحث عن شقة**
- اضغط على "البحث"
- اختر التواريخ والعدد
- شوف الشقق المتاحة

### 3. **الحجز**
- اختر شقة
- اضغط "احجز الآن"
- أدخل بيانات الحجز

### 4. **الدفع**
- استخدم بيانات Stripe التجريبية:
  ```
  رقم البطاقة: 4242 4242 4242 4242
  الشهر: 12
  السنة: 25
  CVC: 123
  ```

---

## 👤 اختبر كصاحب شقة (مالك)

### 1. **إنشاء حساب مالك**
- سجل حساب جديد
- غيّر الدور إلى "مالك"

### 2. **إضافة شقة**
- اذهب إلى "لوحة تحكمي"
- اضغط "إضافة شقة"
- ملأ البيانات

### 3. **رفع الصور**
- في صفحة الشقة
- اضغط "رفع صور"
- اختر صور من جهازك

### 4. **مشاهدة الإحصائيات**
- اذهب إلى "الإحصائيات"
- شوف الحجوزات والإيرادات

---

## 🛠️ الأوامر المهمة الأخرى

```bash
# بناء المشروع للإنتاج
npm run build

# معاينة البناء
npm run preview

# اختبار الكود
npm run test

# فحص الأخطاء
npm run lint

# تنسيق الكود
npm run format
```

---

## 🔧 حل المشاكل الشائعة

### المشكلة: "لا يمكن العثور على الحزم"
```bash
# احذف المجلد والملفات
rm -rf node_modules
rm package-lock.json

# أعد التثبيت
npm install
```

### المشكلة: "CONVEX_DEPLOYMENT غير محدد"
```
تأكد من إضافة CONVEX_DEPLOYMENT في .env.local
```

### المشكلة: "الموقع لا يفتح"
- تأكد من أن المنفذ 5173 متاح
- جرب منفذ مختلف:
```bash
npm run dev -- --port 3000
```

### المشكلة: "البريد الإلكتروني لا يعمل"
- في بيئة التطوير، يتم طباعة البريد في console
- ابحث عن الرابط في الـ terminal

---

## 📊 التحقق من أن كل شيء يعمل

### ✅ البحث عن الشقق
زُر `/apartments` وتأكد من ظهور قائمة

### ✅ الحجز
حاول حجز شقة

### ✅ الدفع
استخدم بيانات Stripe التجريبية

### ✅ الإشعارات
افتح لوحة تحكمك وتحقق من الإشعارات

### ✅ البريد
تحقق من الـ terminal للبريد المُرسل

---

## 🔗 الروابط المهمة

```
الرئيسية: http://localhost:5173/
الشقق: http://localhost:5173/apartments
لوحتي: http://localhost:5173/my-dashboard
حجوزاتي: http://localhost:5173/my-bookings
الإعدادات: http://localhost:5173/settings
```

---

## 💡 نصائح للاختبار

1. **افتح أدوات المطور** (F12)
   - شوف الـ Console للأخطاء
   - شوف الـ Network للطلبات

2. **استخدم Private Window**
   - لاختبار عدة حسابات

3. **افحص البريد الإلكتروني**
   - في terminal ستظهر رسائل البريد

4. **شارك الموقع**
   - من Terminal ستجد رابط Network

---

## 📞 إذا واجهت مشاكل

1. اقرأ الأخطاء في terminal بعناية
2. افحص `.env.local` والتأكد من الإعدادات
3. احذف `node_modules` وأعد التثبيت
4. تأكد من أن Node.js محدث

---

**🎉 استمتع باختبار الموقع! 🚀**
